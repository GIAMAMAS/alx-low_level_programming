#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - determines whether the last digit is greater than 5, 0 or less than 6 but not 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAN
	return (0);
}
#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - determines whether the last digit is greater than 5, 0 or less than 6 but not 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAN
	return (0);
}

